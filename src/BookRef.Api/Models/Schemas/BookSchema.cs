using System;
using BookRef.Api.Models.Queries;
using BookRef.Api.Persistence;
//using GraphQL.Types;

namespace BookRef.Api.Models.Schemas
{
    // public class BookSchema : Schema
    // {
    //     public BookSchema(IServiceProvider provider) : base(provider)
    //     {
    //         Query = (IObjectGraphType)provider.GetService(typeof(BookQuery));
    //     }
    // }
}
